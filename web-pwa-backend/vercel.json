{
  "installCommand": "npm install",
  "buildCommand": "npm run build -w pwa",
  "outputDirectory": "pwa/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
